import {useEffect} from 'react';
export default function Modal({open,onClose,children}){useEffect(()=>{const key=e=>e.key==='Escape'&&onClose?.();if(open)addEventListener('keydown',key);return()=>removeEventListener('keydown',key)},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center overflow-y-auto bg-[#211027]/55 p-4" onMouseDown={onClose}><section role="dialog" aria-modal="true" className="relative my-auto w-full max-w-2xl rounded-xl bg-white p-6 shadow-2xl" onMouseDown={e=>e.stopPropagation()}><button className="absolute right-3 top-2 text-3xl text-slate-500 hover:text-[#79259c]" aria-label="Close" onClick={onClose}>×</button>{children}</section></div>}
